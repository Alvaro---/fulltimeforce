import React from 'react';
import styled from 'styled-components';

const ContainerList = styled.div``;

export const CommitsList = ({ user, rama }) => {
  return <ContainerList>Lista</ContainerList>;
};
